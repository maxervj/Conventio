<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Conventio{% endblock %}</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    {% block stylesheets %}
    <style>
        /* Styles personnalisés pour les messages d'erreur de formulaire */
        .form-error-message {
            color: #dc2626;
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }

        .form-error-icon {
            display: inline-block;
            margin-right: 0.25rem;
        }

        /* Style pour les champs en erreur */
        .form-control.is-invalid {
            border-color: #dc2626 !important;
            background-color: #fef2f2 !important;
        }

        /* Amélioration visuelle des erreurs Symfony */
        .invalid-feedback {
            display: block;
            color: #dc2626;
            font-size: 0.875rem;
            margin-top: 0.5rem;
        }

        .invalid-feedback ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .invalid-feedback li {
            padding: 0.25rem 0;
        }

        .invalid-feedback li:before {
            content: "⚠ ";
            margin-right: 0.25rem;
        }
    </style>
    {% endblock %}
</head>
<body class="bg-gray-50">
    {% block body %}{% endblock %}

    {% block javascripts %}{% endblock %}
</body>
</html>
