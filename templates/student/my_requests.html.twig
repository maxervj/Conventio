{% extends 'base.html.twig' %}

{% block title %}Mes demandes de collecte{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <div class="max-w-6xl mx-auto">
        {# En-tête #}
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">Mes demandes de collecte d'informations</h1>
            <p class="text-gray-600">Suivez l'état de vos demandes de collecte d'informations pour vos conventions de stage.</p>
        </div>

        {# Bouton nouvelle demande #}
        <div class="mb-6">
            <a href="{{ path('student_request_company_info') }}" class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition duration-200 shadow-md">
                <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
                </svg>
                Nouvelle demande
            </a>
        </div>

        {# Liste des demandes #}
        {% if companyInfos|length > 0 %}
            <div class="grid gap-6">
                {% for companyInfo in companyInfos %}
                    <div class="bg-white rounded-xl shadow-md border border-gray-200 overflow-hidden hover:shadow-lg transition duration-200">
                        <div class="p-6">
                            <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-4">
                                {# Informations principales #}
                                <div class="flex-1">
                                    <div class="flex items-start gap-3">
                                        <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-lg flex items-center justify-center text-white flex-shrink-0">
                                            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                                            </svg>
                                        </div>
                                        <div class="flex-1">
                                            <h3 class="text-xl font-bold text-gray-900 mb-2">{{ companyInfo.companyName }}</h3>
                                            <div class="space-y-2 text-sm text-gray-600">
                                                <div class="flex items-center gap-2">
                                                    <svg class="h-4 w-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                                                    </svg>
                                                    <span><strong>Contact :</strong> {{ companyInfo.contactName }}</span>
                                                </div>
                                                <div class="flex items-center gap-2">
                                                    <svg class="h-4 w-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                                                    </svg>
                                                    <span>{{ companyInfo.contactEmail }}</span>
                                                </div>
                                                {% if companyInfo.internshipStartDate and companyInfo.internshipEndDate %}
                                                    <div class="flex items-center gap-2">
                                                        <svg class="h-4 w-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                                                        </svg>
                                                        <span><strong>Période :</strong> du {{ companyInfo.internshipStartDate|date('d/m/Y') }} au {{ companyInfo.internshipEndDate|date('d/m/Y') }}</span>
                                                    </div>
                                                {% endif %}
                                                <div class="flex items-center gap-2">
                                                    <svg class="h-4 w-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                                    </svg>
                                                    <span><strong>Créée le :</strong> {{ companyInfo.createdAt|date('d/m/Y à H:i') }}</span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                {# Statut #}
                                <div class="flex flex-col items-end gap-3">
                                    {% if companyInfo.isCompleted %}
                                        <div class="inline-flex items-center gap-2 px-4 py-2 bg-green-100 text-green-800 rounded-lg font-semibold">
                                            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                            </svg>
                                            Complétée
                                        </div>
                                        {% if companyInfo.completedAt %}
                                            <span class="text-xs text-gray-500">Le {{ companyInfo.completedAt|date('d/m/Y à H:i') }}</span>
                                        {% endif %}
                                    {% elseif companyInfo.isExpired %}
                                        <div class="inline-flex items-center gap-2 px-4 py-2 bg-red-100 text-red-800 rounded-lg font-semibold">
                                            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                            </svg>
                                            Expirée
                                        </div>
                                        {% if companyInfo.expiresAt %}
                                            <span class="text-xs text-gray-500">Expirée le {{ companyInfo.expiresAt|date('d/m/Y') }}</span>
                                        {% endif %}
                                    {% else %}
                                        <div class="inline-flex items-center gap-2 px-4 py-2 bg-yellow-100 text-yellow-800 rounded-lg font-semibold">
                                            <svg class="h-5 w-5 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                            </svg>
                                            En attente
                                        </div>
                                        {% if companyInfo.expiresAt %}
                                            <span class="text-xs text-gray-500">Expire le {{ companyInfo.expiresAt|date('d/m/Y') }}</span>
                                        {% endif %}
                                    {% endif %}
                                </div>
                            </div>

                            {# Informations détaillées si complétées #}
                            {% if companyInfo.isCompleted %}
                                <div class="mt-6 pt-6 border-t border-gray-200">
                                    <button type="button" onclick="toggleDetails{{ companyInfo.id }}()" class="text-blue-600 hover:text-blue-700 font-medium text-sm flex items-center gap-2">
                                        <span id="toggleText{{ companyInfo.id }}">Voir les détails</span>
                                        <svg id="toggleIcon{{ companyInfo.id }}" class="h-4 w-4 transform transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
                                        </svg>
                                    </button>

                                    <div id="details{{ companyInfo.id }}" class="hidden mt-4 grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                                        {% if companyInfo.address %}
                                            <div class="bg-gray-50 p-4 rounded-lg">
                                                <p class="font-semibold text-gray-700 mb-1">Adresse de l'entreprise</p>
                                                <p class="text-gray-600">{{ companyInfo.address }}</p>
                                                {% if companyInfo.addressComplement %}
                                                    <p class="text-gray-600">{{ companyInfo.addressComplement }}</p>
                                                {% endif %}
                                                <p class="text-gray-600">{{ companyInfo.postalCode }} {{ companyInfo.city }}</p>
                                            </div>
                                        {% endif %}

                                        {% if companyInfo.supervisorFirstName %}
                                            <div class="bg-gray-50 p-4 rounded-lg">
                                                <p class="font-semibold text-gray-700 mb-1">Maître de stage</p>
                                                <p class="text-gray-600">{{ companyInfo.supervisorFirstName }} {{ companyInfo.supervisorLastName }}</p>
                                                {% if companyInfo.supervisorFunction %}
                                                    <p class="text-gray-600 text-xs">{{ companyInfo.supervisorFunction }}</p>
                                                {% endif %}
                                                {% if companyInfo.supervisorEmail %}
                                                    <p class="text-gray-600 text-xs">{{ companyInfo.supervisorEmail }}</p>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                    </div>

                                    <script>
                                        function toggleDetails{{ companyInfo.id }}() {
                                            const details = document.getElementById('details{{ companyInfo.id }}');
                                            const icon = document.getElementById('toggleIcon{{ companyInfo.id }}');
                                            const text = document.getElementById('toggleText{{ companyInfo.id }}');

                                            if (details.classList.contains('hidden')) {
                                                details.classList.remove('hidden');
                                                icon.style.transform = 'rotate(180deg)';
                                                text.textContent = 'Masquer les détails';
                                            } else {
                                                details.classList.add('hidden');
                                                icon.style.transform = 'rotate(0deg)';
                                                text.textContent = 'Voir les détails';
                                            }
                                        }
                                    </script>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            {# Aucune demande #}
            <div class="bg-white rounded-xl shadow-md border border-gray-200 p-12 text-center">
                <div class="mx-auto w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mb-4">
                    <svg class="h-10 w-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                    </svg>
                </div>
                <h3 class="text-xl font-semibold text-gray-900 mb-2">Aucune demande de collecte</h3>
                <p class="text-gray-600 mb-6">Vous n'avez pas encore créé de demande de collecte d'informations.</p>
                <a href="{{ path('student_request_company_info') }}" class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition duration-200">
                    <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
                    </svg>
                    Créer ma première demande
                </a>
            </div>
        {% endif %}
    </div>
</div>
{% endblock %}
