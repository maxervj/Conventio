{% extends 'base.html.twig' %}

{% block title %}Modifier les classes{% endblock %}

{% block body %}
    <h1>Modifier les classes que vous enseignez</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_user_info') }}" class="btn btn-secondary">Retour</a>
{% endblock %}
