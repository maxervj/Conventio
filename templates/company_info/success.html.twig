<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ 'company_info.success_title'|trans }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-green-50 to-blue-50 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-2xl w-full bg-white rounded-2xl shadow-xl p-8 md:p-12">
        <div class="text-center">
            {# Icône de succès #}
            <div class="mx-auto w-20 h-20 bg-gradient-to-br from-green-500 to-green-600 rounded-full flex items-center justify-center mb-6 shadow-lg">
                <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/>
                </svg>
            </div>

            {# Titre #}
            <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
                {{ 'company_info.success_title'|trans }}
            </h1>

            {# Message principal #}
            <div class="text-lg text-gray-700 mb-6 leading-relaxed">
                {{ 'company_info.success_message'|trans }}
            </div>

            {# Prochaines étapes #}
            <div class="bg-blue-50 border border-blue-200 rounded-xl p-6 mb-6">
                <h2 class="text-xl font-semibold text-blue-900 mb-3 flex items-center justify-center gap-2">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                    {{ 'company_info.next_steps'|trans }}
                </h2>
                <p class="text-blue-800">
                    {{ 'company_info.convention_info'|trans }}
                </p>
            </div>

            {# Message de remerciement #}
            <div class="text-gray-600 text-base mb-8">
                {{ 'company_info.success_thanks'|trans }}
            </div>

            {# Logo ou signature #}
            <div class="pt-6 border-t border-gray-200">
                <p class="text-sm text-gray-500">Conventio - Lycée Gabriel Fauré</p>
            </div>
        </div>
    </div>
</body>
</html>
